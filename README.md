# "Netrunner-Set-Editor"

*Magic Set Editor* templates/`.mse-game` (~v2.5+) for designing custom *Netrunner* cards (with the new visual assets by *Null Signal Games*). 

## Usage

.

## License

- `data/`: The MSE plugin (`*.mse-include/style`, etc) is under an `MIT` license.
- `assets/`: The assets (frames, symbols, etc) are NSG's under a `CC BY-ND 4.0`.
